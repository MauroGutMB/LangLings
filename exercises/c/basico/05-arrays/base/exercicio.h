#ifndef EXERCICIO_H
#define EXERCICIO_H

/* Imprime os exemplos comentados. O harness de teste nao chama esta funcao:
   a saida dos exemplos ficaria misturada com o resultado das verificacoes. */
void exemplos(void);

/* SUA VEZ: o maior valor entre os n primeiros elementos de v.
   Nunca e chamada com n igual a 0. */
int maior_valor(const int v[], int n);

#endif
