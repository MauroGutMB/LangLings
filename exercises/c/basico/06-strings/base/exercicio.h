#ifndef EXERCICIO_H
#define EXERCICIO_H

/* Imprime os exemplos comentados. O harness de teste nao chama esta funcao:
   a saida dos exemplos ficaria misturada com o resultado das verificacoes. */
void exemplos(void);

/* SUA VEZ: 1 se s comeca com prefixo, 0 caso contrario. */
int comeca_com(const char *s, const char *prefixo);

#endif
