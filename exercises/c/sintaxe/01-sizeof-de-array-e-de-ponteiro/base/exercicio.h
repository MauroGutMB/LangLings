#ifndef EXERCICIO_H
#define EXERCICIO_H

/* Devolve n. v tem n elementos validos; n e passado separado porque, dentro
   da funcao, nao ha como descobrir o tamanho do array so a partir de v. */
int numero_de_elementos(const int v[], int n);

#endif
