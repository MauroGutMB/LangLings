#include "exercicio.h"

/* v, como parâmetro, já decaiu para um ponteiro: não há como recuperar dele
 * quantos elementos o array original tinha. n é o único dado confiável.
 */
int numero_de_elementos(const int v[], int n)
{
    return n;
}
