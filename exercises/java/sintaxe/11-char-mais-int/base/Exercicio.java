// Devolva a letra seguinte a letra no alfabeto.
//
// letra + 1 é um int, não um char — devolver isso direto nem compila. Este
// stub compila trocando o cálculo por um valor que não avança, e por isso
// reprova nos testes.
class Exercicio {
    static char proximaLetra(char letra) {
        return letra; // <- não avança: substitua pelo cálculo certo
    }
}
