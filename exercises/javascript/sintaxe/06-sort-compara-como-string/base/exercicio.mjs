// Devolva uma cópia de arr ordenada em ordem numérica crescente, sem alterar
// o array recebido.
//
// TODO: sort() sem comparador ordena como string, e isso não é ordem numérica.
export function ordenarNumeros(arr) {
  return arr.sort();
}
